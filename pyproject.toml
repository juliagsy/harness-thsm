[project]
name = "decaymem"
version = "0.0.1"
description = "Harness decay without authority creep: typed harness state, dual benchmark, plugin experiment layer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "pyyaml>=6",
    "httpx>=0.27",
    "matplotlib>=3.8",
]

[project.optional-dependencies]
anthropic = ["anthropic>=0.50"]
mem0 = ["mem0ai>=0.1.100", "sentence-transformers>=3.0"]
arc = ["continual-arc"]

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["decaymem"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.uv.sources]
continual-arc = { path = "../continual-arc", editable = true }
